# SmartSaver App

React Native (Expo) IoT control app. See repo root for full documentation.

```bash
yarn start       # Expo dev server
yarn android     # Build + run native Android
yarn lint        # ESLint check
npx tsc --noEmit # TypeScript check
```

**Docs:** `../AGENTS.md`, `./CONTEXT.md`, `../fix-instructions.md`
